{ pkgs,  ... }: {
  # services."06cb-009a-fingerprint-sensor" = {
  #   enable = true;
  #   backend = "python-validity";
  # };
  #
  # security.pam.services = {
  #   login.fprintAuth = true;     # TTY login
  #   sudo.fprintAuth = true;      # sudo fingerprint
  #   su.fprintAuth = true;      # sudo fingerprint
  #   passwd.fprintAuth = true;      # sudo fingerprint
  #   gdm.fprintAuth = true;       # graphical login (if using GNOME)
  #   polkit-1.fprintAuth = true;
  # };
}
